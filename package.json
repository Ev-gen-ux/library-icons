{
  "name": "library-icons",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build-icons.ts",
    "clean": "tsx scripts/clean.ts",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
